# Use-Case Diagram

```mermaid
graph TD
    %% Імпорт історії транзакцій
    A2[Student] --> |association|A2_1(Імпортувати історію транзакцій);
    A2_1 -.->|include| A2_11(Завантажити виписку CSV/XLSX/PDF);
    A2_1 -.->|include| A2_12(Підтвердити імпорт);
    A2_11 -.->|extend| A2_111(Переглянути імпортовані транзакції);
    A2_11 -.->|extend| A2_111(Редагувати транзакції перед імпортом);
 
    
    B2[Застосунок] -->|association| B2_1(Обробити імпорт історії транзакцій);
    B2_1 -.->|include| B2_11(Отримати виписку);
    B2_1 -.->|include| B2_12(Зберегти транзакції у базі даних);
    B2_11 -.->|include| B2_111(Розпізнати структуру виписки);
    B2_11 -.->|include| B2_112(Відобразити виписку);
    B2_12 -.->|include| B2_121(Валідація виписки)
    
    
    A2_12 -->|realization| B2_12;
    A2_11 -->|realization| B2_11;
    A2_111 -->|realization| B2_112;
