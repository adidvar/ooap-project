# Use-Case Diagram

```mermaid
graph TD
    %% Імпорт історії транзакцій
    A2[Student] --> A2_1(Імпортувати історію транзакцій);
    A2_1 -->|include| A2_11(Завантажити виписку CSV/XLSX/PDF);
    A2_11 -->|include| A2_12(Розпізнати структуру файлу);
    A2_12 -->|include| A2_13(Переглянути імпортовані транзакції);
    A2_13 -->|extend| A2_14(Редагувати транзакції перед імпортом);
    A2_13 -->|include| A2_15(Підтвердити імпорт);
    
    B2[Система] --> B2_1(Обробити імпортовані дані);
    B2_1 -->|include| B2_11(Перевірити коректність даних);
    B2_1 -->|include| B2_12(Зберегти транзакції у внутрішній системі);
    
    A2_15 -->|association| B2_1;
    A2_14 -->|association| A2_13;
