# Use-Case Diagram

```mermaid
graph TD
    %% Імпорт історії транзакцій
    A2[Student] --> |association|A2_1(Імпортувати історію транзакцій);
    A2_1 -.->|include| A2_11(Завантажити виписку);
    A2_1 -.->|include| A2_12(Підтвердити імпорт);
    A2_11 -.->|extend| A2_111(Переглянути імпортовані транзакції);
    A2_11 -.->|extend| A2_112(Редагувати транзакції перед імпортом);
