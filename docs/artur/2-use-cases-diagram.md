# Use-Case Diagram

```mermaid
graph TD
    %% Імпорт історії транзакцій
    A2[Student] -->|Натискає 'Імпортувати виписку'| B2(Завантаження файлу CSV/XLSX/PDF);
    B2 --> |Система аналізує структуру файлу| C2[Розпізнавання даних транзакцій];
    C2 --> D2[Попередній перегляд імпортованих транзакцій];
    D2 -->|Підтверджує імпорт| E2[Дані збережено у внутрішній системі];

    %% Альтернативні потоки
    D2 -->|Змінює файл перед імпортом| F2[Повторний аналіз даних];

    %% Виключні ситуації
    B2 -->|Файл містить помилки| X2[Помилка: Неправильний формат файлу];
    C2 -->|Файл не містить транзакцій| Y2[Помилка: Немає доступних даних];

    %% Результат
    E2 --> Z2[Транзакції успішно імпортовані];
