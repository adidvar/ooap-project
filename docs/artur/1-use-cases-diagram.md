# Use-Case Diagram

```mermaid
graph TD;

    %% Фінансовий чат-бот консультант
    B[Чат-бот] --> |association|B1(Відповісти на запит);
    B1 -.->|include| B11(Згенерувати відповідь на запит);
    B1 -.->|include| B12(Відобразити відповідь на запит у чаті);

    A[Student] --> |association|A1(Отримати фінансову консультацію);
    A1 -.->|extend| A11a(Переглянути історію запитів);
    A1 -.->|include| A111(Зробити запит на консультацію);
    A111 -.->|extend| A111a(Відредагувати запит);
    A111 -.->|include| A1111(Написати запит);

    A1111 --> |realization| B1;
    A11a --> |realization| B12;

