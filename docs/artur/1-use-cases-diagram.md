# Use-Case Diagram

```mermaid
graph TD;

    %% Фінансовий чат-бот консультант
    A[Student] -->|Натискає 'Консультація'| B(Відкриття фінансового чат-бота);
    B --> |Вводить запит| C[Чат-бот аналізує питання];
    C --> D[Чат-бот надає відповідь];
    D --> E[Користувач переглядає історію діалогу];

    %% Альтернативні потоки
    D -->|Переформульовує запит| F[Повторний запит до чат-бота];

    %% Виключні ситуації
    B -->|Чат-бот не розпізнав запит| X[Помилка: Невідома тема];

    %% Результат
    D --> Z[Користувач отримав фінансову консультацію];
