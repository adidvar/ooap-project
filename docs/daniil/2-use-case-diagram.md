```mermaid
graph TD;

    A[Student] -->|Додає запланований платіж| B(Введення дати та суми платежу);
    B -->|Натискає 'Зберегти'| C[Система зберігає платіж та налаштовує нагадування];
    C --> D[Notification System перевіряє наближення дати платежу];
    D --> E[Користувач отримує push-сповіщення за 3 дні до оплати];
    D --> F[Користувач отримує нагадування у день оплати];

    %% Альтернативні потоки
    D -->|Змінює або видаляє нагадування| G[Оновлення або видалення нагадування];

    %% Виключні ситуації
    B -->|Немає інтернету| X[Помилка: Сповіщення не надіслано через відсутність з'єднання];

    %% Результат
    F --> Z[Користувач отримує сповіщення про платіж];
