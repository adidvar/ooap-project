```mermaid
graph TD;

    A[Student] -->|Натискає 'Додати витрату'| B(Введення суми, категорії, дати);
    B --> |Натискає 'Зберегти'| C[Система додає витрату до фінансового балансу];
    C --> D[Користувач переглядає оновлений баланс];

    %% Альтернативні потоки
    D -.->|extend| E[Оновлення даних витрати];
    D -.->|extend| F[Витрата видалена];

    %% Виключні ситуації
    B -.->|include| X[Помилка: Від'ємне число];
    B -.->|include| Y[Помилка: Категорія обов'язкова];

    %% Результат
    C --> Z[Баланс оновлений]; 
