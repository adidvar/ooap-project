```mermaid
graph TD;

    A[Student] -->|Натискає 'Додати витрату'| B(Введення суми, категорії, дати);
    B --> |Натискає 'Зберегти'| C[Система додає витрату до фінансового балансу];
    C --> D[Користувач переглядає оновлений баланс];

    %% Альтернативні потоки
    D -->|Редагує витрату| E[Оновлення даних витрати];
    D -->|Видаляє витрату| F[Витрата видалена];

    %% Виключні ситуації
    B -->|Некоректна сума| X[Помилка: Від'ємне число];
    B -->|Відсутня категорія| Y[Помилка: Категорія обов'язкова];

    %% Результат
    C --> Z[Баланс оновлений];
