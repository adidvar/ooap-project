```mermaid
graph TD;

    A[Student] -->|Додає запланований платіж| B(Введення дати та суми платежу);
    B -->|Натискає 'Зберегти'| C[Система зберігає платіж та налаштовує нагадування];
    C -->|Очікує дату платежу| D[Notification System перевіряє наближення дати];

    %% Основний потік
    D -->|3 дні до оплати| E[Користувач отримує push-сповіщення];
    D -->|В день оплати| F[Користувач отримує нагадування];

    %% Альтернативні потоки
    D -.->|extend| G[Редагування або видалення нагадування];

    %% Виключні ситуації
    E -.->|include| X[Перевірка інтернет-з'єднання];
    F -.->|include| X;
    X -->|Сповіщення не надіслано| Y[Помилка: Відсутнє з'єднання];

    %% Результат
    F -->|Нагадування отримано| Z[Користувач отримав сповіщення про платіж];


